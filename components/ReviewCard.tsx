import React from 'react';

const ReviewCard = ({review}: { review: object }) => {
    return (
        <div>
            <h1>
                ReviewCard
            </h1>
        </div>
    );
};

export default ReviewCard;