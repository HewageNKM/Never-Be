import React from 'react';
import {useRouter} from "next/router";

const Phrase = () => {
    const router = useRouter();
    return (
        <div>
            <h1>Search</h1>
        </div>
    );
};

export default Phrase;